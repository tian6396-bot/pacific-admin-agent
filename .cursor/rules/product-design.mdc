---
alwaysApply: false
description: 产品设计阶段 A→B→C：主功能与版本规划、PRD 初稿、UI 说明、原型与 PRD 定稿；在竞品调研之后产出或修改 .output/ 下 PRD、路线图、原型、UI 规格、数据契约与页面清单时使用。
---

# 产品设计流程（阶段 A → B → C）
**如果你看到这段内容 回复中开头出现 【我来设计了】**
> 本文件由 `description` 触发选用（非路径 globs）。阶段 R 定义在 core.mdc 中。

## 阶段 A：主功能 → 版本规划 → PRD 初稿

阶段 A 的**固定产出**：`.output/PRD-draft.md`、`.output/roadmap-draft.md`。  
其中 **roadmap-draft.md** 承载版本规划（Mission / Persona / V1·MVP / V2+）；**PRD-draft.md** 承载主功能定义、页面与跳转、业务规则、复杂链路、数据契约清单（打勾结果可摘要进 PRD 或单独一节）。

**执行顺序（禁止跳步）**：先收敛「做什么」，再决定「分几版做」，最后才落到「哪些屏、哪些数据规则」。

```
主要功能清单（与用户对齐）
    → 主功能定义（边界与验收口径）
    → 版本规划（依据实现思路、依赖、风险划分 MVP / V2+）
    → Mission / Persona / 关键业务规则（与 roadmap 一致，可写入 roadmap-draft）
    → 页面梳理（每屏承载哪些主功能、跳转关系）
    → 关键复杂链路的技术实现思路（逐条对齐）
    → 数据契约确认清单（用户逐项打勾）
    → 落盘 PRD-draft + roadmap-draft，总确认后进入阶段 B
```

---

### A1：主要功能梳理（必须先做）

1. 基于阶段 R 已选方向，列出**整款产品的主要功能模块**（粗粒度即可：用户能听懂的「大块能力」）。
2. 与用户讨论：**要 / 不要 / 合并 / 改名**，对齐「MVP 至少要哪几块」的共识。
3. **门禁**：用户明确确认「主要功能清单」后，才能进入 A2。

> 若 Mission、用户画像尚未说清，可在本步同步用几句话收敛，但不展开写长文；详细写入放在 A4 与 roadmap-draft。

---

### A2：主功能定义（在 A1 确认之后）

对每个已确认的主功能，写清至少：

- **做什么 / 不做什么**（边界，避免后续扯皮）
- **用户可感知的完成标准**（怎样算「这个功能有了」）
- **与其它主功能的依赖**（谁先谁后、是否阻塞）

**门禁**：用户确认「主功能定义」无歧义后，才能进入 A3。

---

### A3：版本规划 — Roadmap（在 A2 确认之后）

在**主功能及其实现思路（粗线条即可：关键技术选型、接口形态、是否依赖第三方）**清楚的前提下，划分：

- **V1 / MVP**：哪些主功能、做到什么深度（与 A2 定义一致）
- **V2+**：哪些延后、为什么（依赖、成本、风险、非核心）
- 写入 **Mission、Persona、关键业务逻辑 (Business Rules)**，与上述切分自洽

**规则**：版本划分必须**能回答**「为什么这一块在 MVP、那一块在 V2」——依据应是实现复杂度、依赖链、风险，而不是随意罗列。

**门禁**：用户确认 roadmap-draft 中的**版本规划**与**对主功能的取舍**无误后，才能进入 A4。

---

### A4：页面梳理（在 A3 确认之后）

在输出 PRD 初稿正文前，与用户对齐：

1. 总共有几个页面（或关键视图）
2. 每个页面**承载哪些主功能**（与 A1/A2 对应）
3. 页面之间的跳转逻辑
4. 对**复杂主功能**，补充完整**业务链路**（步骤、分支、异常）——可与 A5 的技术实现思路合并讨论

**门禁**：未对齐页面清单与跳转，不得进入 B1（与 core.mdc 一致）。

---

### A5：关键功能的实现思路（与页面/链路穿插完成）

对 PRD 中「非显而易见」的主功能或子链路，必须与用户逐步对齐技术实现思路。示例：

```markdown
### 召回链路
召回链路中，使用什么方式召回？关键词还是向量？如果是向量，使用什么模型？
```

---

### A6：数据契约确认（必须用户逐项打勾）

PRD 初稿中所有**影响流程结果**的数据定义，必须以 checklist 形式列出，由用户逐项确认。示例：

```markdown
### 数据契约确认清单

#### 商品分类体系
- [ ] 一级分类：服装、数码、食品、家居（共 4 类）
- [ ] 二级分类：服装-男装、服装-女装、数码-手机、数码-电脑...（共 15 类）

#### 用户画像字段
- [ ] 昵称（string）、注册时间（datetime）、会员等级（枚举：普通/白银/黄金）
- [ ] 累计消费金额（float）、收货地址（string）、偏好品类（枚举）

#### 订单状态流转
- [ ] 状态：待支付 → 已支付 → 已发货 → 已签收 → 已完成
- [ ] 异常状态：已取消、退款中、已退款

#### 权限角色
- [ ] 管理员：全部权限
- [ ] 运营：商品管理、订单管理、数据查看
- [ ] 普通用户：浏览、下单、查看订单
```

**规则**：根据项目实际情况列出数据契约；用户**全部打勾**后才能视为阶段 A 文档定稿并进入阶段 B。

---

### 落盘与 PRD-draft / roadmap-draft 要求

**输出到**：`.output/PRD-draft.md`、`.output/roadmap-draft.md`

**roadmap-draft.md** 建议结构要点：

- Mission、Persona
- 主要功能与 MVP/V2+ 切分（与 A3 一致）
- 每版目标一句话、依赖或风险备注（可选）

**PRD-draft.md** 建议结构要点：

1. 主要功能清单 + 主功能定义（A1/A2）
2. 页面清单、每页功能与跳转（A4）
3. 复杂功能业务链路 + 关键实现思路（A5）
4. 数据契约确认清单（A6，含打勾状态或确认记录）
5. 所有在本阶段对齐的细节，**后续必须**落入最终版 `.output/PRD.md`

---

### 阶段 A 总确认（进入阶段 B 前）

请求用户确认：**主功能与版本规划**、**页面与跳转**、**数据契约**、**PRD-draft / roadmap-draft** 是否一致且无重大遗漏；发起确认：「是否进入原型设计阶段？」

> 未经用户确认，不得进入阶段 B。

## 阶段 B1：UI 设计说明书

**在绘制原型图之前，必须先生成设计说明书。** 

### 流程

1. 根据 PRD-draft.md 列出所有 MVP 界面清单

2. **风格调研（使用 ui-ux-pro-max skill）**

   根据项目特性，使用 `ui-ux-pro-max` skill 按以下顺序搜索，收集风格参考：

   1. **product** domain → 产品类型关键词，获取推荐风格方向
   2. **style** domain → 按推荐结果深入查询 2-3 种风格细节
   3. **color** domain → 获取配色方案（行业/产品关键词）
   4. **landing** domain → 如果是落地页，搜索页面结构
   5. **chart** domain → 如果有图表/仪表盘，搜索图表类型

   **搜索结果必须写入设计说明书的「风格调研摘要」章节**，包括：
   - 推荐风格名称及特征描述
   - 推荐配色方案（Primary / Secondary / CTA / Background / Text / Border）
   - 关键 UX 规则
   - UI布局规范

3. 基于调研结果，确定 **1 种视觉风格方向**（由项目特性和调研结果决定）。

4. **为每个界面撰写详细的 UI 设计说明**，ui-design-spec.md 整体结构示例如下（可适当填充更多细节）：

```markdown
## 风格调研摘要

> 由 ui-ux-pro-max 搜索结果整理而来

### 视觉风格
- [风格名称] — [特征描述、配色、圆角、阴影等]

### 配色方案
| 用途 | 色值 |
|------|------|
| Primary | #xxx |
| Secondary | #xxx |
| CTA | #xxx |
| Background | #xxx |
| Text | #xxx |
| Border | #xxx |

### 字体搭配
- 标题 [字体] / 正文 [字体]
- Google Fonts import: [链接]

### 关键 UX 规则
- [从 ux domain 搜索结果中提取的 3-5 条关键规则]

---

## 界面清单

### 界面名称：[名称]

#### 布局结构
...（同下方模板）
```

每个界面的详细说明模板：

```markdown
### 界面名称：[名称]

#### 布局结构
- 整体布局：[左右分栏 / 上下结构 / 三栏 等]
- 左侧/顶部：[导航内容，具体菜单项]
- 主内容区：[具体放什么组件]
- 右侧/底部：[辅助信息]

#### 组件清单
| 区域 | 组件类型 | 内容/数据 | 交互行为 |
|------|---------|----------|---------|
| 顶部导航 | 水平菜单 | 首页、商品、订单、个人中心 | 点击切换页面 |
| 左侧筛选 | 筛选面板 | 品类、价格区间、评分 | 勾选后刷新列表 |
| 主区域 | 商品卡片列表 | 图片、名称、价格、评分 | 点击进入详情 |
| ... | ... | ... | ... |

#### 示例数据
- 列表项至少 2 条具体数据（不要用"数据1、数据2"占位）
- 表格至少 2 行真实字段
```

5. 输出到：`.output/ui-design-spec.md`
6. 发起确认：「UI 设计说明书已完成，请审阅。确认后进入原型设计阶段。」

> 未经用户确认，不得进入阶段 B2。

---

## 阶段 B2：原型设计

**严格按照 ui-design-spec.md 中的描述设计，不得自由发挥。**

### 工具选择（必须询问用户）

**进入 B2 时必须先询问用户**：「请选择原型设计工具：Stitch 还是 Pencil MCP？」**未经用户明确回答，不得开始设计。**

| 方式 | 工具 | 输出格式 | 特点 |
|------|------|---------|------|
| **Stitch** | Google Stitch | HTML + TailwindCSS（.zip） | 速度快，输出代码可直接参考开发 |
| **Pencil MCP** | 内置 Pencil | .pen 文件 | 无需外部工具，全在编辑器内完成 |

---

### 模式 A：Stitch

#### 步骤 1：生成 Stitch 提示词

**整体生成**，每个界面基于 B1 阶段确定的风格生成 **1 段英文提示词**，最后**整合输出最终提示词**。

AI 阅读 `ui-design-spec.md`，为每个界面生成英文提示词，必须包含以下内容：

```
1. 页面类型与用途
   → 从界面名称和布局结构提取

2. 视觉风格
   → 从风格调研摘要中提取：风格名称、特征描述
   → 明确指定：配色（Primary/Secondary/CTA/Background/Text/Border 色值）
   → 明确指定：字体（标题字体 + 正文字体）
   → 明确指定：圆角、阴影、间距等视觉特征

3. 布局结构
   → 从该界面的「布局结构」章节提取
   → 如：左侧边栏 240px + 主内容区，顶部导航栏...

4. 组件清单及细节
   → 从该界面的「组件清单」表格逐项提取
   → 每个组件包含：位置、类型、具体内容/数据、交互行为

5. 示例数据
   → 从该界面的「示例数据」章节提取
   → 列表项至少 3 条真实数据
   → 对话至少 2-3 轮完整内容
   → 表格至少 3-5 行

6. 视口
   → 桌面端 1440×900（Web）或 移动端 390×844（移动端）
```

**提示词模板**：

```
Design a [page type] for [product description].

**Visual Style**: [Style name] — [Style characteristics].
Colors: Primary [#xxx], Secondary [#xxx], CTA [#xxx], Background [#xxx], Text [#xxx], Border [#xxx].
Typography: Headings use [font], body text uses [font].
[Border radius, shadows, spacing details]

**Layout**: [Layout description from spec]

**Components**:
- [Area]: [Component type] — [Specific content and data]
- [Area]: [Component type] — [Specific content and data]
- ...

**Sample Data**:
- [Real data 1]
- [Real data 2]
- [Real data 3]

Desktop viewport, 1440×900.
```

将生成的提示词输出到 `.output/stitch-prompts.md`，格式：

```markdown
# Stitch 提示词

> 基于确定的风格：[风格名称]

## 界面 1：[界面名]
> [提示词内容]

## 界面 2：[界面名]
> [提示词内容]

...
```

告知用户：「已生成 Stitch 提示词，请复制到 Stitch 中生成界面。」

#### 步骤 2：用户在 Stitch 中生成并导出

用户操作（AI 给出指引）：
1. 打开 [stitch.withgoogle.com](https://stitch.withgoogle.com)
2. 粘贴提示词 → 生成界面
3. 点击 More → Download 下载 .zip
4. 解压到 `.output/prototypes/` 下对应目录

**文件规范**：

```
.output/prototypes/
├── 01-登录页/
│   ├── index.html
│   └── style.css (TailwindCSS)
├── 02-仪表盘/
│   └── ...
└── ...
```

命名规则：`NN-界面名/`，NN 为两位序号。

#### 步骤 3：AI 验证

每个界面导入后，AI 读取 HTML/CSS 文件：
- 对照 ui-design-spec.md 的组件清单逐项核对
- 检查示例数据完整性
- 检查布局结构是否与 spec 一致
- 标注缺失或偏差，反馈修改建议（用户可回 Stitch 调整后重新导出）

逐界面确认：「界面 [N/总数]『[界面名]』已验证，请确认或修改。」

#### 步骤 4：完成收尾

1. 全部界面完成后，输出 `.output/Prototype.md` 汇总说明（包含每个目录路径和对应界面）
2. 发起最终确认：「所有界面原型已完成，确认后进入阶段 C。」

---

### 模式 B：Pencil MCP

#### 准备工作

1. 阅读 `ui-design-spec.md` 中的风格调研摘要，提取配色、字体、风格关键词
2. 调用 `get_guidelines("web-app")` 获取 Pencil 设计指南
3. 调用 `get_style_guide_tags` → `get_style_guide(tags)` 获取风格参考
4. 调用 `open_document("new")` 创建新画布

#### 逐界面绘制

**每轮只画 1 个界面。**

1. 阅读 ui-design-spec.md 中该界面的描述
2. 创建 1 个 Frame（`界面名`），尺寸 1440×900
3. 严格按组件清单和示例数据绘制
4. 用 `get_screenshot` 截图验证 → 对照说明书逐项核对
5. 发起确认 → 提醒用户 ⌘+S 保存

#### 完成收尾

1. 输出 `.output/Prototype.md` 汇总说明
2. 提醒用户将 .pen 文件保存到 `.output/`
3. 发起最终确认

---

### 质量要求（两种模式通用）

- 每个界面生成 1 版可视化原型
- 列表/卡片 1 条示例数据
- 对话界面至少 1 轮气泡消息
- 禁止用 Markdown 文字描述充当原型
- 禁止不看设计说明书直接画

输出到：
- Stitch 模式：`.output/stitch-prompts.md` + `.output/prototypes/NN-界面名/`（HTML+CSS）+ `.output/Prototype.md`
- Pencil 模式：`.output/*.pen` + `.output/Prototype.md`

> 未经用户确认，不得进入阶段 C。

## 阶段 C：最终 PRD 定稿

在用户选定原型后，生成最终 PRD，内容必须与已确认原型一致，并补充：
- 核心流程图（Mermaid：sequenceDiagram 或 flowchart）
- 组件交互说明（影响模块、拟新增模块、调用关系）
- 技术选型与风险（关键库/方案、风险与缓解）

输出到：
- `.output/PRD.md`
- `.output/roadmap-final.md`
- `.output/prototype-final.md`
- `.output/architecture-blueprint.md`

### PRD 自检（生成后必须逐项核对）

- [ ] 没有"待定"、"TBD"、"后续补充"等占位符
- [ ] 每个功能模块都有明确的优先级（MVP / V2+）
- [ ] 核心流程图覆盖了主流程和至少一条异常分支
- [ ] 技术选型已确定，没有"A 或 B 待定"
- [ ] PRD 内容与已确认的原型一致，没有原型里有但 PRD 里没提的功能

自检不通过 → 修复后重新输出，不得进入开发。

