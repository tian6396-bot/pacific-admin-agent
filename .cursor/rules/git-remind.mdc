---
description: 每次完成代码/文档修改后，提醒用户提交并推送到 GitHub
alwaysApply: true
---

# 修改后提醒 Git 同步

完成一轮会改动仓库文件的任务后（创建、修改、删除代码或文档），在回复末尾**简短提醒**用户：

1. 本地改动尚未自动同步到 GitHub
2. 需要时可以说「提交并推送」，由助手执行 `git add` / `commit` / `push`
3. 勿提交密钥：`backend/.env` 等已在 `.gitignore` 中的文件

若本轮只有问答、未改文件，则不必提醒。

提醒保持一两句话即可，不要打断主结论。
